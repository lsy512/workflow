# buildozer.spec 文件示例内容（根据需要调整）
[app]
title = My Application
package.name = myapplication
package.domain = org.test
version = 0.1
requirements = python3,kivy==2.0.0,android,plyer,kivymd # 根据需要添加其他依赖。确保版本兼容。
orientation = portrait,landscape # 应用支持的方向。根据需要调整。
sdk = 21 # Android SDK版本。根据需要调整。
minsdk = 21 # Android最小SDK版本。根据需要调整。